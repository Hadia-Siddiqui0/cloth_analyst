# Render.com deploy config -- point Render at this repo and it reads this
# file automatically. Alternative: Railway also works fine with just the
# Dockerfile, no extra config needed there.
services:
  - type: web
    name: textile-bi-backend
    runtime: python
    region: singapore
    plan: free
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: textile-bi-db
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production
      - key: CORS_ORIGINS
        value: '["https://cloth-analyst.vercel.app"]'

databases:
  - name: textile-bi-db
    plan: free
    region: singapore